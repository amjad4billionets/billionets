[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[dev]
  command = "npm run dev"
  port = 8888        # URL you open in the browser (http://localhost:8888)
  targetPort = 8080  # Vite dev server port from vite.config.ts
